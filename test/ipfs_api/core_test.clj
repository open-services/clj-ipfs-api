;; (ns ipfs-api.core-test
;;   (:require [clojure.test :refer :all]
;;             ;; [ipfs-api.core :as ipfs-api]
;;             ))
;; 
;; (deftest daemon-control
;;   (testing "starts daemon"
;;     (let [connection (ipfs-api/daemon)]
;;       (is (not (nil? connection))))))
